import type { NextConfig } from "next";

/**
 * AI-FDE is a standalone product. It runs on its own port with no basePath,
 * no multi-zone rewrites, and no dependency on any sibling workspace.
 */
const config: NextConfig = {};

export default config;
